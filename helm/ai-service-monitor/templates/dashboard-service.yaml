apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-service-monitor.fullname" . }}-dashboard
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "ai-service-monitor.dashboard.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ai-service-monitor.dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.dashboard.port }}
      targetPort: http
      protocol: TCP
