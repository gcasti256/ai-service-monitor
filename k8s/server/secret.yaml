apiVersion: v1
kind: Secret
metadata:
  name: server-secrets
  namespace: ai-monitor
  labels:
    app.kubernetes.io/name: server
    app.kubernetes.io/part-of: ai-service-monitor
type: Opaque
stringData:
  # REQUIRED: Replace with real values before applying.
  # Do NOT commit real secrets to source control.
  # Consider using Sealed Secrets or External Secrets Operator.
  API_KEY: ""
  DATABASE_URL: ""
  ALERT_WEBHOOK_URLS: ""
