apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: ai-monitor
  labels:
    app.kubernetes.io/name: dashboard
    app.kubernetes.io/part-of: ai-service-monitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
